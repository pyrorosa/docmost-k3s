apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: docmost
spec:
  selector:
    app: grafana
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      name: web
      nodePort: 30300
