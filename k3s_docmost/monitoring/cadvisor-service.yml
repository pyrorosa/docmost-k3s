apiVersion: v1
kind: Service
metadata:
  name: cadvisor
  namespace: docmost
  labels:
    app: cadvisor
spec:
  selector:
    app: cadvisor
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
