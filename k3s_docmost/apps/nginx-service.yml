apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: docmost
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      name: http
      nodePort: 30080
